VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_TELEGRAM_BOT_NAME=

# The SHA-256 hash of your bot's token, represented as a lowercase hex string.
# This is used to verify the authenticity of the data returned by the
# Telegram login widget. Compute it by taking SHA-256 of your bot token
# (e.g. using OpenSSL: `echo -n "$BOT_TOKEN" | sha256sum`) and
# removing the trailing `-`. Do not commit your raw bot token.
VITE_TELEGRAM_BOT_TOKEN_HASH=
